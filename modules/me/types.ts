export type ProyectDataItem = {
  title: string;
  description: string;
  tags: string[];
  link: string;
}

export type ExperienceDataItem = {
  company: string,
  rol: string,
  period: string,
  tasks: Array<string>
}