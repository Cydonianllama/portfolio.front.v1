/* eslint-disable @typescript-eslint/no-explicit-any */
import { api } from '@/setup/axios'
import { ResponseApi } from '@/types/api/response'

export const Method = async (): Promise<ResponseApi<any> | null> => {
  try {
    const req = await api.get(``)
    return null;
  } catch (ex) {
    return null
  }
}